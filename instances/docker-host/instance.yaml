# OpenClaw Docker Host Configuration
# Multi-instance Docker host running multiple OpenClaw containers

name: docker-host
display_name: OpenClaw Docker Host
type: docker  # Indicates this is a Docker-based deployment

# AWS Configuration
aws:
  profile: serova
  region: af-south-1

# Host Instance Configuration (larger for running multiple containers)
instance:
  type: t3.large     # 8GB RAM - good for 5-10 bots
  volume_size: 50    # More storage for Docker images and volumes

# Bots to deploy (each gets isolated Docker volumes)
bots:
  - name: iris
    port: 18791
  - name: uhura
    port: 18792
  # Add more bots as needed:
  # - name: pepper
  #   port: 18789
  # - name: river
  #   port: 18790
  # - name: alfred
  #   port: 18793
  # - name: jarvis
  #   port: 18794

# SSH Configuration (single key for the host)
ssh:
  key_name: pepper_docker_host_key
  key_path: ~/.ssh/pepper_docker_host_key
  public_key_path: ~/.ssh/pepper_docker_host_key.pub

# Networking
network:
  allowed_ssh_cidr: "196.45.30.215/32"
  vpc_cidr: "10.200.0.0/16"
  subnet_cidr: "10.200.1.0/24"

# Paths
paths:
  backup_dir: ~/.openclaw-docker-backups

# Tags
tags:
  Owner: theunisdk
  Project: pepper
  Environment: prod
